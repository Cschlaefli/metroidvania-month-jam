using Godot;
using System;

public static class Damage
{
    const int none = 0;
    const int fear = 1;
    const int freeze = 2;

   public enum dam_types {None = none, Fear = fear, Freeze = freeze }
}
